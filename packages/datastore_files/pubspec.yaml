name: datastore_files
description: Data store implementations for file operations (YAML loading/writing).
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0-0 <4.0.0"

dependencies:
  fpdart: ^1.2.0
  yaml: ^3.1.2
  domain_contracts: any
  domain_entities: any

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6